# 🍴 Forking Workflow

## 📚 Apa itu Forking Workflow?

Forking workflow merupakan salah satu strategi git branching yang populer dalam project OpenSource.

Forking adalah mekanisme menduplikasi repository, biasanya hal ini dilakukan karena contributor tidak memiliki akses untuk melakukan perubahan ke repository utama.

---

## 🎯 Konsep Dasar

### 🔄 Perbedaan dengan Workflow Lainnya

Forking Workflow sangat berbeda dari Git workflow populer lainnya. Alih-alih menggunakan satu repository di server sebagai "pusat" kode, workflow ini memberikan **setiap developer repository server mereka sendiri**.

**Artinya:**
- Setiap kontributor memiliki **DUA** repository Git:
  - 🏠 **Repository lokal pribadi** (di komputer Anda)
  - ☁️ **Repository server publik** (di GitHub/Bitbucket)

### ✨ Keuntungan Utama

- ✅ Kontribusi dapat diintegrasikan **tanpa** perlu semua orang push ke satu repository pusat
- ✅ Developer push ke repository server **mereka sendiri**
- ✅ **Hanya project maintainer** yang bisa push ke repository resmi
- ✅ Maintainer dapat menerima commit dari developer manapun **tanpa** memberikan akses write ke codebase resmi
- ✅ Memberikan cara kerja terdistribusi yang fleksibel untuk tim besar
- ✅ **Ideal untuk project open source**

---

## 🚀 Cara Kerja Step by Step

### Langkah 1: 🍴 Developer Fork Repository Resmi

Developer membuat **fork** (salinan) dari repository resmi ke server mereka sendiri. Ini menciptakan **repository publik pribadi** mereka.

**Catatan Penting:**
- ❌ Developer lain **tidak bisa** push ke repository ini
- ✅ Developer lain **bisa** pull perubahan dari repository ini

### Langkah 2: 💻 Clone Fork ke Lokal

Setelah fork dibuat, developer melakukan `git clone` untuk mendapatkan salinan ke komputer lokal mereka.

```bash
git clone https://user@bitbucket.org/user/repo.git
```

**Output:**
```
Cloning into 'repo'...
remote: Counting objects: 100, done.
remote: Compressing objects: 100% (90/90), done.
Receiving objects: 100% (100/100), done.
```

Ini berfungsi sebagai **lingkungan development pribadi** mereka.

### Langkah 3: 🔗 Tambahkan Remote untuk Repository Resmi

Developer perlu menambahkan path remote untuk repository 'resmi':

```bash
git remote add upstream https://bitbucket.org/maintainer/repo
```

**Penjelasan:**
- `origin` = remote untuk **fork repository Anda** (otomatis dibuat saat clone)
- `upstream` = remote untuk **repository resmi/official**

**Output saat cek remote:**
```bash
git remote -v
```
```
origin    https://user@bitbucket.org/user/repo.git (fetch)
origin    https://user@bitbucket.org/user/repo.git (push)
upstream  https://bitbucket.org/maintainer/repo (fetch)
upstream  https://bitbucket.org/maintainer/repo (push)
```

### Langkah 4: 🌿 Buat Branch Fitur Baru

```bash
git checkout -b some-feature
```

**Output:**
```
Switched to a new branch 'some-feature'
```

### Langkah 5: ✏️ Lakukan Perubahan

Developer membuat perubahan pada branch baru tersebut.

```bash
# Edit beberapa file
git commit -a -m "Add first draft of some feature"
```

**Output:**
```
[some-feature abc1234] Add first draft of some feature
 2 files changed, 50 insertions(+), 5 deletions(-)
```

**Catatan:** Semua perubahan ini sepenuhnya **pribadi** hingga di-push ke repository publik mereka.

### Langkah 6: 🔄 Update dari Repository Resmi (Opsional)

Jika project resmi sudah berkembang, developer bisa mengakses commit baru dengan:

```bash
git pull upstream main
```

**Output:**
```
From https://bitbucket.org/maintainer/repo
 * branch            main       -> FETCH_HEAD
Already up to date.
```

### Langkah 7: ⬆️ Push Branch ke Repository Publik Sendiri

```bash
git push origin some-feature
```

**Output:**
```
Counting objects: 5, done.
Writing objects: 100% (5/5), 500 bytes, done.
To https://user@bitbucket.org/user/repo.git
 * [new branch]      some-feature -> some-feature
```

**Perbedaan Penting:** 
- Remote `origin` menunjuk ke **repository server pribadi developer**, bukan ke codebase utama

### Langkah 8: 📝 Buat Pull Request

Developer membuka **pull request** dari branch baru ke repository resmi.

**Fungsi Pull Request:**
- 📢 Memberi tahu project maintainer bahwa ada update siap diintegrasikan
- 💬 Menyediakan thread diskusi jika ada masalah dengan kode yang dikontribusikan

### Langkah 9: ✅ Pull Request Disetujui dan Di-merge

Maintainer meng-approve dan merge pull request ke repository server-side resmi.

**Proses Maintainer:**
1. Pull perubahan kontributor ke repository lokal mereka
2. Cek untuk memastikan tidak merusak project
3. Merge ke branch `main` lokal mereka
4. Push branch `main` ke repository resmi di server

Sekarang kontribusi sudah menjadi bagian dari project! 🎉

---

## 🔍 Perbedaan: Forking vs Cloning

### 🍴 Forking
- Repository yang "di-fork" dibuat menggunakan perintah standar `git clone`
- Biasanya **"server-side clones"**
- Umumnya dikelola oleh layanan Git pihak ketiga seperti GitHub atau Bitbucket
- ❌ **Tidak ada** perintah Git unik untuk membuat forked repository

### 📋 Cloning
- Pada dasarnya operasi **copy dari repository dan history-nya**

---

## 🌿 Branching dalam Forking Workflow

Semua repository publik pribadi ini adalah cara yang nyaman untuk **berbagi branch** dengan developer lain.

**Yang Perlu Diingat:**
- 👥 Semua orang tetap harus menggunakan **branch** untuk mengisolasi fitur individual
- 🔄 Perbedaannya hanya pada **cara branch dibagikan**:
  - **Forking Workflow:** Branch di-**pull** ke repository lokal developer lain
  - **Feature Branch/Gitflow Workflow:** Branch di-**push** ke repository resmi

---

## 📝 Ringkasan Lengkap Forking Workflow

### 🎬 Contoh Skenario Real:

**1.** 🎯 Anda ingin berkontribusi ke library open source yang di-hosting di:
```
bitbucket.org/userA/open-project
```

**2.** 🍴 Menggunakan Bitbucket, Anda membuat fork repository ke:
```
bitbucket.org/YourName/open-project
```

**3.** 💻 Di sistem lokal Anda, eksekusi `git clone`:
```bash
git clone https://bitbucket.org/YourName/open-project
```

**4.** 🌿 Anda membuat branch fitur baru di repository lokal:
```bash
git checkout -b new-feature
```

**5.** ✏️ Pekerjaan selesai untuk menyelesaikan fitur baru dan `git commit` dieksekusi:
```bash
git commit -m "Complete new feature"
```

**6.** ⬆️ Anda push branch fitur baru ke remote fork repository Anda:
```bash
git push origin new-feature
```

**7.** 📝 Menggunakan Bitbucket, Anda membuka pull request untuk branch baru terhadap repository asli di:
```
bitbucket.org/userA/open-project
```

---

## 🎯 Kapan Menggunakan Forking Workflow?

### ✅ Cocok Untuk:
- 🌟 **Project open-source publik** (paling umum)
- 🏢 **Workflow bisnis privat** untuk kontrol authoritative lebih besar atas apa yang di-merge ke release
- 👥 Tim yang memiliki **Deploy Managers**
- 📅 Tim dengan **release cycle yang ketat**

### 💡 Keuntungan untuk Maintainer:
- Membuka repository untuk kontribusi dari developer manapun
- **Tanpa** perlu mengelola authorization settings secara manual untuk setiap kontributor individual
- Memberikan maintainer workflow bergaya **"pull"** yang lebih terkontrol

---

## 📖 Referensi Lebih Lanjut

🔗 [Atlassian - Comparing Workflows: Forking Workflow](https://www.atlassian.com/git/tutorials/comparing-workflows/forking-workflow)

---

**💡 Tips:** Tidak yakin workflow mana yang tepat untuk Anda? Pelajari karakteristik project Anda dan pilih workflow yang paling sesuai dengan struktur tim dan kebutuhan kolaborasi Anda!
