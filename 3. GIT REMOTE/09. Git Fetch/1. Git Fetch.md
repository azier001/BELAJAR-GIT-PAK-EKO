# 📥 Git Fetch - Panduan Lengkap untuk Pemula

## 🎯 Apa Itu Fetch?

**Fetch** adalah perintah di Git yang digunakan untuk **mendapatkan perubahan terakhir** dari remote repository.

### 🤔 Mengapa Fetch Penting?

- **Cek Perubahan**: Berguna ketika kita ingin mengetahui perubahan apa saja yang sudah terjadi pada remote repository
- **Kolaborasi Tim**: Mungkin rekan kerja kita sudah menambah perubahan ke remote repository
- **Aman**: Fetch **tidak akan mengubah** isi local Git Project kita

> 💡 **Catatan Penting**: Fetch hanya mengunduh informasi perubahan, tapi tidak langsung menggabungkannya ke project lokal kita!

---

## 🚀 Melakukan Fetch

### 📋 Fetch Semua Branch

Untuk melakukan fetch untuk **semua branch** di remote repository, gunakan perintah:

```bash
git fetch namaremote
```

**Contoh Penggunaan**:
```bash
git fetch origin
```

**Penjelasan**:
- `namaremote` biasanya bernama `origin` (nama default untuk remote repository)
- Perintah ini akan mengunduh semua perubahan dari semua branch yang ada di remote

---

### 🎯 Fetch Branch Tertentu

Jika kita ingin melakukan fetch untuk **branch tertentu** saja di remote, gunakan:

```bash
git fetch namaremote remotebranch
```

**Contoh Penggunaan**:
```bash
git fetch origin main
```

**Penjelasan**:
- `namaremote` adalah nama remote repository (biasanya `origin`)
- `remotebranch` adalah nama branch yang ingin kita fetch (contoh: `main`, `develop`, dll)
- Lebih efisien jika kita hanya butuh update dari satu branch saja

---

## 📝 Tugas Praktik

Mari praktikkan penggunaan fetch dengan simulasi kolaborasi tim! 🎓

### ✅ Langkah 1: User Pertama - Buat Perubahan
**Di komputer user pertama**:
- Lakukan commit perubahan pada project
- Push perubahan ke remote repository

```bash
git add .
git commit -m "Menambahkan fitur baru"
git push origin main
```

---

### ✅ Langkah 2: User Kedua - Fetch Perubahan
**Di komputer user kedua**:
- Lakukan fetch untuk mendapatkan informasi perubahan terbaru

```bash
git fetch origin
```

**Apa yang terjadi?**
- Git akan mengunduh informasi perubahan dari remote
- File lokal kita **belum berubah**
- Perubahan tersimpan di referensi remote (seperti `origin/main`)

---

### ✅ Langkah 3: User Kedua - Cek Perbedaan
**Di komputer user kedua**:
- Lakukan git diff untuk melihat perbedaan antara branch local dan branch remote

```bash
git diff main origin/main
```

**Penjelasan**:
- `main` = branch lokal kita
- `origin/main` = branch remote yang baru di-fetch
- Perintah ini akan menampilkan apa saja yang berbeda antara keduanya

---

## 🎓 Kesimpulan

✨ **Fetch** adalah cara aman untuk:
- 🔍 Mengecek perubahan di remote repository
- 👥 Mengetahui apa yang dikerjakan tim
- 🛡️ Melihat perubahan sebelum menggabungkannya ke project lokal

> 💪 **Tips**: Biasakan melakukan `git fetch` sebelum memulai pekerjaan untuk memastikan kita tahu perubahan terbaru dari tim!

---

**Selamat Belajar! 🚀**
