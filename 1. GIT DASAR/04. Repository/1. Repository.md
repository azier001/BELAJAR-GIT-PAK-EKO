# 📚 Git Dasar - Repository

## 🎯 Apa itu Repository?

**Repository** adalah tempat penyimpanan project di Git. Bayangkan repository seperti sebuah folder ajaib yang dapat melacak semua perubahan yang terjadi pada file-file di dalamnya.

### 📝 Hal Penting tentang Repository:

- 🗂️ Repository merupakan sebutan project di Git
- 📁 Kita bisa membuat folder kosong atau folder yang sudah berisi file, lalu membuatnya sebagai Git Repository
- ☁️ Atau kita bisa melakukan clone Git Repository yang sudah ada dari Server Git

---

## 🚀 Membuat Repository

Membuat repository sangat mudah! Kita hanya perlu menggunakan satu perintah sederhana.

### 📌 Langkah-langkah:

- ⌨️ Untuk membuat repository, kita hanya perlu menggunakan perintah: `git init`
- 📂 Dan dilakukan dari dalam folder yang akan kita jadikan sebagai Git Repository
- 👀 Setelah membuat Git Repository, kita bisa lihat ada folder baru dengan nama `.git`
- ⚠️ `.git` merupakan folder yang berisikan database Git, jangan sampai kita mengubah data yang terdapat pada folder `.git`

> **💡 Tips:** Folder `.git` adalah "otak" dari repository kamu. Jangan pernah menghapus atau mengubah isinya secara manual!

---

## 💻 Kode : Git Init

Mari kita praktikkan cara membuat repository pertama kamu!

```bash
git init
```

**Output:**
```bash
Initialized empty Git repository in /path/to/your/project/.git/
```

### 📖 Penjelasan:
Perintah `git init` akan membuat folder `.git` di dalam direktori kamu. Setelah perintah ini dijalankan, folder kamu sudah menjadi Git Repository! 🎉

---

## 🔍 Kode : Git Status

Setelah membuat repository, kita perlu tahu status dari repository tersebut. Gunakan perintah `git status` untuk melihatnya.

```bash
git status
```

**Output:**
```bash
On branch master

No commits yet

nothing to commit (create/copy files and use "git add" to track)
```

### 📖 Penjelasan:
- 🌿 `On branch master` → Kita sedang berada di branch (cabang) bernama master
- 📭 `No commits yet` → Belum ada commit (belum ada perubahan yang disimpan)
- 📝 `nothing to commit` → Belum ada file yang ditambahkan untuk dilacak oleh Git

> **💡 Tips untuk Pemula:** Perintah `git status` adalah sahabat terbaik kamu! Gunakan sesering mungkin untuk mengetahui kondisi repository kamu. Ini seperti "mengecek kesehatan" project kamu! 🏥

---

## 🎓 Rangkuman

✅ Repository adalah tempat penyimpanan project Git  
✅ Gunakan `git init` untuk membuat repository baru  
✅ Folder `.git` menyimpan semua data Git (jangan diubah!)  
✅ Gunakan `git status` untuk mengecek kondisi repository  

**Selamat! Kamu sudah belajar cara membuat dan mengecek repository Git!** 🎊
