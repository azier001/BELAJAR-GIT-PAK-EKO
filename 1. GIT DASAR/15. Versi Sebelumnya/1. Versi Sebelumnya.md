# 📚 Versi Sebelumnya

## 🎯 Apa itu Versi Sebelumnya?

Pernahkah kamu mengalami situasi dimana kamu sudah mengubah file, tapi ternyata versi sebelumnya lebih baik? Tenang, Git punya solusinya! 🎉

### 💡 Kenapa Fitur Ini Penting?

- Kadang kita sering mengalami masalah dengan file yang sudah kita commit ke Repository
- Git memiliki fitur dimana kita bisa melihat versi file pada commit sebelumnya
- Saat kita ambil versi file sebelumnya, file pada commit tersebut akan berada di **Staging Index**
- Kita bisa membatalkan perubahan yang tidak diinginkan dengan mudah! ✨

---

## 🔧 Cara Menggunakan

Untuk mengambil versi file dari commit sebelumnya, gunakan perintah:

```bash
git checkout hash -- namafile
```

**Penjelasan:**
- `hash` = Kode unik dari commit yang ingin kita ambil filenya
- `namafile` = Nama file yang ingin kita kembalikan ke versi sebelumnya

---

## 📋 Contoh Kasus: Git Status

Mari kita lihat contoh nyata! Misalkan kita ingin melihat isi **file1.txt** sebelum terjadi perubahan di commit `1b5f564`. 

### 🔍 Langkah 1: Cek History Commit

Pertama, kita lihat dulu history commit dengan perintah `git log --oneline`:

```bash
a26ec92 (HEAD -> master) Add file3.txt
b34dc9a mengubah file2.txt menjadi file_2.txt
ae4a58c menghapus file3.txt
1b5f564 perubahan di file1.txt dan file2.txt
2118ac3 perubahan di file3.txt
59272c6 menambah file3.txt
4f15c9b menambah file2.txt
7453f5e Menambah file1.txt
```

**📖 Penjelasan Output:**
- Setiap baris menunjukkan satu commit
- Angka di depan (contoh: `a26ec92`) adalah **hash commit** (kode unik)
- `(HEAD -> master)` menunjukkan posisi kita saat ini
- Teks setelah hash adalah **pesan commit** yang menjelaskan perubahan

### 📌 Identifikasi Commit yang Diinginkan

Dari log di atas, kita lihat bahwa:
- Commit `1b5f564` = perubahan terbaru di file1.txt
- Commit `2118ac3` = versi sebelum perubahan tersebut

Jadi kalau kita ingin kembalikan **file1.txt** ke kondisi sebelum commit `1b5f564`, kita gunakan commit `2118ac3`! 🎯

---

## 💻 Contoh Praktik: Git Checkout

Sekarang mari kita praktikkan! Kita akan mengambil **file1.txt** dari commit `2118ac3`:

```bash
belajar-git-dasar git:(master) git log --oneline
➜ belajar-git-dasar git:(master) git checkout 2118ac3 -- file1.txt
➜ belajar-git-dasar git:(master) ✗ git status
On branch master
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        modified:  file1.txt

➜ belajar-git-dasar git:(master) ✗
```

### 🔍 Apa yang Terjadi?

1. **Perintah pertama** (`git log --oneline`): Melihat daftar commit
2. **Perintah kedua** (`git checkout 2118ac3 -- file1.txt`): Mengambil file1.txt dari commit 2118ac3
3. **Perintah ketiga** (`git status`): Mengecek status perubahan

### 📊 Hasil yang Didapat:

- File **file1.txt** sekarang sudah kembali ke versi lama (versi di commit 2118ac3)
- File tersebut otomatis masuk ke **Staging Index** (siap untuk di-commit)
- Status menunjukkan `modified: file1.txt` yang artinya file sudah berubah

### ⚠️ Catatan Penting:

- Tanda `✗` di sebelah `git:(master)` menunjukkan ada perubahan yang belum di-commit
- File yang di-checkout otomatis masuk staging area
- Jika ingin membatalkan, gunakan `git restore --staged file1.txt`

---

---

## 🔄 Bagaimana Jika Hanya Ingin Melihat Saja?

Kadang kita hanya ingin **melihat** perubahan versi lama, tapi **tidak jadi menggunakannya**. Bagaimana cara kembali ke commit terakhir?

### 📝 Cek Status Setelah Checkout

Setelah melakukan checkout versi lama, jika kita ketik `git status`:

```bash
On branch master
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        modified:  file1.txt
```

**📖 Arti Status:**
- File **file1.txt** sudah ada di **staging area** (hijau, siap di-commit)
- Status: **modified** (file terdeteksi berubah dari commit terakhir)
- Git sudah siap untuk menyimpan perubahan ini jika kita commit

### 🔙 Cara Membatalkan dan Kembali ke Commit Terakhir

Ada beberapa cara untuk membatalkan dan kembali ke kondisi commit terakhir:

#### **Cara 1: Langkah Bertahap (Untuk Pemula)** 👶

**Langkah 1** - Keluarkan file dari staging area:
```bash
git restore --staged file1.txt
```

**Langkah 2** - Buang perubahannya:
```bash
git restore file1.txt
```

**Penjelasan:**
- Perintah pertama: Mengeluarkan file dari staging area (dari hijau ke merah)
- Perintah kedua: Membuang perubahan dan kembali ke versi commit terakhir

---

#### **Cara 2: Langsung Sekali Jalan (Lebih Cepat)** ⚡

```bash
git restore --staged --worktree file1.txt
```

**Penjelasan:**
- `--staged`: Keluarkan dari staging area
- `--worktree`: Buang perubahan di working directory
- Dengan satu perintah, file langsung kembali seperti semula!

---

#### **Cara 3: Menggunakan Checkout ke HEAD** 🎯

```bash
git checkout HEAD -- file1.txt
```

**Penjelasan:**
- `HEAD` = Pointer yang menunjuk ke **commit terakhir**
- Perintah ini akan mengembalikan file ke kondisi commit terakhir
- File kembali seperti semula (sebelum kita checkout versi lama)

---

### 🔍 Verifikasi Hasil

Setelah menggunakan salah satu cara di atas, cek kembali dengan:

```bash
git status
```

**Output yang diharapkan:**
```bash
On branch master
nothing to commit, working tree clean
```

**Artinya:**
- ✅ Tidak ada perubahan yang pending
- ✅ File sudah kembali ke versi commit terakhir
- ✅ Working directory bersih (clean)

---

### 💡 Analogi Sederhana

Bayangkan kamu sedang membaca buku:

1. **Git Checkout (versi lama)** = Membuka halaman lama untuk dibaca
2. **Git Status** = Melihat bookmark yang menandai halaman tersebut
3. **Git Restore/Checkout HEAD** = Menutup buku dan kembali ke halaman terakhir yang kamu baca

---

## 🎓 Kesimpulan

Dengan fitur **Git Checkout**, kamu bisa:
- ✅ Mengambil versi file dari commit manapun
- ✅ Melihat perubahan versi lama tanpa harus menyimpannya
- ✅ Membatalkan perubahan yang tidak diinginkan dengan mudah
- ✅ Kembali ke commit terakhir kapan saja

### 📌 Ringkasan Perintah Penting:

| Perintah | Fungsi |
|----------|--------|
| `git checkout hash -- file` | Ambil file dari commit tertentu |
| `git status` | Cek status perubahan file |
| `git restore --staged file` | Keluarkan file dari staging area |
| `git restore file` | Buang perubahan file |
| `git checkout HEAD -- file` | Kembali ke versi commit terakhir |

**Tips untuk Pemula:** 
Jangan takut mencoba! Selalu gunakan `git status` untuk melihat kondisi file sebelum dan sesudah melakukan perintah. Git tidak akan menghapus history commit kamu! 🚀
