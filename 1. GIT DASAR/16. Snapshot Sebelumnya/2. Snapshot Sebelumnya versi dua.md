# 📚 Git Dasar - Panduan untuk Pemula

## ⏰ Snapshot Sebelumnya

### 🎯 Apa itu Snapshot?
Snapshot adalah kondisi/keadaan project kita pada waktu tertentu. Bayangkan seperti foto - setiap kali kita commit, Git mengambil "foto" dari kondisi project kita saat itu.

### 🚀 Fitur Mesin Waktu Git
Git memiliki fitur seperti mesin waktu yang sangat berguna! Dengan fitur ini:
- ✨ Kita bisa **kembali ke kondisi project sebelumnya**
- 🔍 Kita bisa **melihat bagaimana project kita di masa lalu**
- 🛡️ Kita bisa **mengembalikan perubahan yang salah**

### 📍 Menentukan Tujuan dengan Hash Commit
Setiap snapshot memiliki identitas unik yang disebut **hash commit**. Hash ini seperti alamat yang menunjukkan lokasi snapshot tertentu.

**Contoh hash commit:** `7453f5e`, `a1b2c3d`, dll.

---

## 🎯 Cara Berpindah ke Snapshot Tertentu

### 📝 Perintah Dasar
Untuk menuju ke snapshot tertentu, gunakan perintah:

```bash
git checkout hash
```

**Penjelasan:**
- `git checkout` = perintah untuk berpindah
- `hash` = ganti dengan hash commit yang ingin dituju

**Contoh penggunaan:**
```bash
git checkout 7453f5e
```

### 🏠 Kembali ke Posisi Awal (Branch Utama)

Jika sudah selesai melihat-lihat snapshot lama dan ingin kembali ke posisi terbaru:

```bash
git checkout namabranch
```

**Contoh:**
```bash
git checkout master
```

**💡 Tips:** Biasanya nama branch utama adalah `master` atau `main`

---

## 🌿 Git Branch

### ❓ Apa itu Branch?
**Branch** adalah cabang pengembangan dalam Git. Bayangkan seperti pohon:
- 🌳 **Batang utama** = branch utama (master/main)
- 🌿 **Cabang** = branch untuk fitur atau eksperimen baru

### 🎁 Branch Default
Saat pertama kali membuat Git Repository, secara **otomatis** Git akan membuat branch default (biasanya bernama `master` atau `main`).

### 🔎 Melihat Nama Branch Aktif

Untuk mengetahui kita sedang berada di branch mana, gunakan perintah:

```bash
git branch --show-current
```

**Penjelasan perintah:**
- `git branch` = perintah untuk mengelola branch
- `--show-current` = opsi untuk menampilkan branch yang sedang aktif

**📺 Contoh Output:**
```bash
➜ belajar-git-dasar git:(master) git branch --show-current
master
➜ belajar-git-dasar git:(master)
```

**Artinya:** Kita sedang berada di branch bernama `master`

---

## 💻 Kode: Git Checkout (Contoh Lengkap)

### 📋 Langkah-langkah Detail

**1️⃣ Melihat daftar commit:**
```bash
git log --oneline
```

**2️⃣ Berpindah ke commit tertentu:**
```bash
git checkout 7453f5e
```

### 📺 Output yang Akan Muncul:

```bash
➜ belajar-git-dasar git:(master) git log --oneline
➜ belajar-git-dasar git:(master) git checkout 7453f5e
Note: switching to '7453f5e'.

You are in 'detached HEAD' state. You can look around, make experimental
changes and commit them, and you can discard any commits you make in this
state without impacting any branches by switching back to a branch.

If you want to create a new branch to retain commits you create, you may
do so (now or later) by using -c with the switch command. Example:
```

### 🤔 Apa Arti Pesan di Atas?

**"Detached HEAD state"** artinya:
- 👀 Kita sedang melihat snapshot lama
- ✏️ Kita bisa melakukan eksperimen/perubahan
- 🔄 Perubahan tidak akan mempengaruhi branch utama
- ↩️ Kita bisa kembali ke branch utama kapan saja

**⚠️ Penting untuk Pemula:**
Ini adalah mode "lihat-lihat saja". Jika ingin membuat perubahan permanen, sebaiknya buat branch baru terlebih dahulu (akan dipelajari di materi selanjutnya).

---

## 🏠 Kode: Kembali ke Commit Terakhir

### 📋 Langkah Kembali ke Branch Utama

Setelah selesai melihat-lihat snapshot lama, kita perlu kembali ke commit terakhir (terbaru). Caranya sangat mudah!

**Perintah:**
```bash
git checkout master
```

### 📺 Output yang Akan Muncul:

```bash
➜ belajar-git-dasar git:(7453f5e) git checkout master
Previous HEAD position was 7453f5e Menambah file1.txt
Switched to branch 'master'
➜ belajar-git-dasar git:(master)
```

### 📖 Penjelasan Output:

**Baris 1:** `Previous HEAD position was 7453f5e`
- Git memberi tahu posisi terakhir kita (snapshot lama yang tadi kita lihat)
- `7453f5e` adalah hash commit yang tadi kita kunjungi

**Baris 2:** `Menambah file1.txt`
- Ini adalah pesan commit dari snapshot `7453f5e`
- Membantu kita mengingat apa yang terjadi di commit tersebut

**Baris 3:** `Switched to branch 'master'`
- ✅ Konfirmasi bahwa kita sudah berhasil kembali ke branch `master`
- Sekarang kita sudah berada di commit terakhir (terbaru)

**Baris 4:** `git:(master)`
- Prompt berubah dari `git:(7453f5e)` menjadi `git:(master)`
- Menandakan kita sudah kembali ke branch utama

### 🎯 Kesimpulan

**Sebelum checkout:** `git:(7453f5e)` → Sedang melihat snapshot lama
**Setelah checkout:** `git:(master)` → Kembali ke commit terakhir/terbaru

💡 **Ingat:** Setiap kali selesai melihat snapshot lama, selalu gunakan `git checkout master` (atau nama branch utama Anda) untuk kembali ke kondisi terbaru!

---

## 📌 Rangkuman

| Perintah | Fungsi |
|----------|--------|
| `git checkout <hash>` | Berpindah ke snapshot tertentu |
| `git checkout <branch>` | Kembali ke branch tertentu |
| `git branch --show-current` | Melihat branch yang aktif |
| `git log --oneline` | Melihat daftar commit (untuk mencari hash) |

---

## 💡 Tips untuk Pemula

✅ **DO's (Lakukan):**
- Gunakan `git log --oneline` untuk melihat daftar hash commit sebelum checkout
- Catat nama branch utama Anda sebelum berpindah
- Kembali ke branch utama setelah selesai melihat-lihat

❌ **DON'Ts (Jangan):**
- Jangan melakukan commit saat dalam mode "detached HEAD" jika Anda masih pemula
- Jangan panik jika melihat pesan "detached HEAD" - ini normal!

---

## 🎯 Latihan untuk Pemula

**Coba langkah-langkah ini:**

1. Lihat daftar commit Anda: `git log --oneline`
2. Catat hash commit yang paling lama
3. Pindah ke commit tersebut: `git checkout <hash>`
4. Lihat isi file Anda (akan terlihat seperti versi lama)
5. Kembali ke branch utama: `git checkout master`
6. Lihat isi file lagi (akan kembali ke versi terbaru)

**🎉 Selamat!** Anda sudah belajar cara menggunakan mesin waktu Git!

---

**📚 Catatan:** Materi branching yang lebih mendalam akan dibahas pada course terpisah. Untuk sekarang, fokus dulu pada pemahaman dasar ini.
