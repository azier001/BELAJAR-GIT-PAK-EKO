# 🔧 Amend Commit

## ❓ Apa Itu Amend Commit?

Amend commit adalah fitur Git yang memungkinkan Anda **mengubah atau menambahkan perubahan pada commit terakhir** tanpa perlu membuat commit baru. Ini sangat berguna saat Anda lupa menambahkan file atau lupa memperbaiki sesuatu sebelum melakukan commit.

---

## 📋 Kapan Digunakan?

- 🚫 **Ketika sudah melakukan commit**, mungkin ada beberapa hal yang terlupakan (file, kode, atau perubahan)

- 🔄 **Biasanya tanpa amend**, kita harus melakukan reset soft ke commit sebelumnya, lalu tambahkan perubahan yang terlupakan, dan lakukan commit ulang. Ini terasa merepotkan!

- ✨ **Dengan amend**, semua itu bisa dilakukan lebih mudah hanya dengan satu perintah: `git commit --amend`

- ⚠️ **Perlu diingat**, amend akan **mengubah hash commit** karena data perubahan yang dicommit bertambah. Ini berarti commit lama akan diganti dengan commit baru yang memiliki identitas berbeda.

---

## 💻 Kode : Git Commit Amend

Berikut adalah contoh cara menggunakan `git commit --amend` untuk menambahkan file yang terlupakan:

### **Langkah 1️⃣ : Tambahkan File ke Staging Area**

```bash
git add file3.txt
```

**Penjelasan**: Perintah ini menambahkan file `file3.txt` ke staging area, siap untuk dicommit.

---

### **Langkah 2️⃣ : Lakukan Amend Commit**

```bash
git commit --amend -m "Add file3.txt"
```

**Penjelasan**: 
- `--amend` : Mengubah commit terakhir
- `-m` : Menetapkan pesan commit
- `"Add file3.txt"` : Pesan yang baru untuk commit yang diamend

---

### **Output Hasil:**

```
[master a26ec92] Add file3.txt
Date: Sat Jun 19 22:54:32 2021 +0700
1 file changed, 2 insertions(+)
create mode 100644 file3.txt
```

**Penjelasan Output**:
- ✅ `[master a26ec92]` : Hash commit baru (berbeda dari sebelumnya)
- 📝 `Add file3.txt` : Pesan commit yang baru
- 📅 `Date` : Tanggal dan waktu commit
- 📊 `1 file changed` : 1 file yang berubah
- ➕ `2 insertions(+)` : 2 baris konten ditambahkan
- 🆕 `create mode 100644 file3.txt` : File baru dibuat dengan permission 100644

---

## 🎯 Ringkasan

Amend commit adalah cara cepat untuk **memperbaiki atau melengkapi commit terakhir** tanpa harus membuat commit baru yang terlihat berantakan di history Git. Gunakan dengan bijak, terutama jika Anda belum push ke repository remote! 🚀
