# ⚙️ Configuration

## 📝 Pengenalan

Setelah selesai menginstall Git, hal yang pertama kali kita lakukan adalah melakukan **konfigurasi**. Konfigurasi ini penting agar Git mengenali siapa yang melakukan perubahan pada project.

---

## 👤 Mengatur User Name dan Email

Yang paling utama yang perlu kita konfigurasi di awal adalah **user name** dan **user email**. Informasi ini akan tercatat di setiap perubahan yang kita buat.

```bash
~ git config --global user.name "Your Name"
~ git config --global user.email "your.email@example.com"
~ 
```

### 💡 Penjelasan:
- `--global` artinya konfigurasi ini berlaku untuk semua project Git di komputer kita
- `user.name` adalah nama kita yang akan muncul di history Git
- `user.email` adalah email kita yang akan muncul di history Git

---

## 💻 Menggunakan Visual Studio Code

Agar mempermudah, kita akan menjadikan **Visual Studio Code** sebagai default editor untuk Git dan default diff tool.

### 🔧 Setting Editor Default

```bash
~ git config --global core.editor "code --wait"
~ 
```

### 💡 Penjelasan:
- `core.editor` mengatur editor default untuk Git
- `"code --wait"` memberitahu Git untuk menggunakan VS Code dan menunggu hingga kita selesai mengedit

---

### 🔍 Setting Diff Tool

```bash
~ git config --global diff.tool "default-difftool"
~ git config --global difftool.default-difftool.cmd "code --wait --diff \$LOCAL \$REMOTE"
~ 
```

### 💡 Penjelasan:
- `diff.tool` mengatur tool untuk melihat perbedaan antara file
- Perintah ini memungkinkan kita melihat perbedaan file menggunakan VS Code
- `\$LOCAL` adalah file versi lokal kita
- `\$REMOTE` adalah file versi yang akan dibandingkan

---

## 📋 Melihat Seluruh Configuration

Untuk melihat semua konfigurasi Git yang sudah kita buat, gunakan perintah berikut:

```bash
~ git config --list --show-origin
~ 
```

### 💡 Penjelasan:
- `--list` menampilkan semua konfigurasi Git
- `--show-origin` menampilkan dari mana konfigurasi tersebut berasal (file mana yang menyimpannya)

---

## ✅ Checklist Konfigurasi Awal

Pastikan kamu sudah melakukan hal-hal berikut:

- ✓ Set user name
- ✓ Set user email  
- ✓ Set editor default (opsional, tapi sangat disarankan)
- ✓ Set diff tool (opsional, tapi sangat membantu)
- ✓ Cek semua konfigurasi dengan `git config --list`

---

> **💭 Tips:** Konfigurasi ini cukup dilakukan sekali saja di awal. Setelah itu, Git akan selalu menggunakan konfigurasi yang sama untuk semua project!
